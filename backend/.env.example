# Porta do servidor (opcional, padrão 4000)
PORT=4000

# Credenciais de Basic Auth para rotas protegidas
ADMIN_USER=admin
ADMIN_PASS=admin

# Conexão com o banco de dados MySQL
# Substitua USER, PASSWORD, HOST, PORT e DATABASE pelos seus valores
DATABASE_URL="mysql://root@localhost:3306/leads_db"
